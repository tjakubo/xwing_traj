function traj = straight(baseSize, resolution, currTraj, speed)
    strLen = [40 80 120 160 200];
    len = strLen(speed)+baseSize;
    step = len/resolution;
    for k=1:1:(ceil(step))
        
    end
end